@model IEnumerable<TestAfLogin.Models.ProfilePictureModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameOfUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imageName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.NameOfUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imageName)
            </td>*@
            <td>
                @Html.DisplayFor(ModelItem => item.NameOfUser)
            </td>
            <td>
                <img src="~/image/@item.imageName" width="400" height="400" asp-append-version="true"/>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PictureId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PictureId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PictureId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
